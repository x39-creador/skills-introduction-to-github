## 🔐 Autenticación (X39 Matrix Token)

### Endpoints protegidos
Todos los endpoints Matrix CS/S2S requieren header `X-39-Token` para acceso descentralizado via ICP Matrix protocol.

**Header requerido:**